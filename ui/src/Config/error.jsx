const ERROR = {
    ACCORDION: {
        ACCORDION_DATA_GET_FAILS:
            "Carga fallida"
    },
    CLIENTS: {
        CLIENTS_DATA_GET_FAILS:
            "Carga fallida"
    }
}

export { ERROR }